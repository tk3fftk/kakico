{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "{ git diff -z --name-only HEAD 2>/dev/null; git ls-files -z --others --exclude-standard 2>/dev/null; } | grep -z '\\.swift$' | xargs -0 swiftlint lint --strict --quiet >&2 || exit 2"
          }
        ]
      }
    ]
  }
}
